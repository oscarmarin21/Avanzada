# Copy this file to .env and fill in your values.
# The backend task will load .env when starting Spring Boot (if the file exists).

# OpenAI API key (required for AI suggestions and summary).
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Set to true to enable AI features (suggest type/priority, summary).
# Spring Boot maps APP_AI_ENABLED to app.ai.enabled.
APP_AI_ENABLED=false

# Optional: override endpoint and model (defaults: OpenAI API, gpt-3.5-turbo).
# OPENAI_API_URL=https://api.openai.com/v1/chat/completions
# OPENAI_MODEL=gpt-3.5-turbo
